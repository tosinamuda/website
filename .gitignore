# build output
dist

# dependencies
node_modules

# macOS noise
.DS_Store

# vale style packages (fetched by `vale sync`); config + vocab stay tracked
.vale/*
!.vale/config/

# local Claude Code settings
.claude

# local browser-tool artefacts
.playwright-cli

# working folder for reference designs / scratch
tmp
